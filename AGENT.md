**ERP AI Assistant：项目开发与协作约定**

本文件用于帮助开发人员与协作代理理解项目背景、当前技术决策和验证方式。项目入口见 [README](README.md)，完整设计见 [项目方案](docs/erp-ai-assistant-plan.md)。明确区分已实现功能与规划功能，新增业务代码时同步维护对应说明。

**项目介绍与当前状态**

平台连接上游供应商和下游采购企业，覆盖多个行业，已有 ERP 和 SQL Server 业务数据库。AI 助手作为独立项目建设，先为平台内部管理者与运营人员提供销售、企业、供应商和商品分析。

产品入口包括经营看板、今日 Top 5、AI 问数和运营待办。每条重点事项应保留事实依据、统计时间、建议动作、负责人和处理状态；没有足够事项时如实展示实际数量。

当前已有销售订单分析后端、React + TypeScript 三页经营看板、锁定依赖和后端／浏览器测试。已只读连接 QY 备份还原后的本地 `ERP_Local`，数据所有者确认备份覆盖全平台商家。同时保留原始订单与暂定有效销售指标，不代表支付成交额。助手数据库、增量同步、任务调度、AI、Top 5、运营待办和 CI 尚未实现。实际映射见 [数据字典](docs/data-dictionary.md)，所有未确定业务细节统一维护在 [业务假设台账](docs/business-assumptions.md)。

用户已授权按常识采用可调整默认规则继续开发，无需因一般业务口径未确认而停工。默认纳入订单完成、已出库，CNY 和 Asia/Shanghai 暂定；规则在 `backend/config/business-rules.json`，新快照保存规则全文、版本和指纹。修改口径必须更新台账、配置、相关测试并重算新快照，不能静默修改旧结果。缺少支付事件或商家映射时不能编造对应事实。

**当前技术决策**

| 范围 | 决策与约束 |
|---|---|
| 架构 | 独立项目、前后端分离，取数、计算、接口和展示分工明确 |
| 源数据库 | 本地 SQL Server 17.0.1000.7、ERP_Local、兼容级别 100；697 表；业务口径与线上部署待确认 |
| 后端 | Python + FastAPI |
| 计算 | 当前仅使用 pandas 作为数据计算框架 |
| 前端 | Web + React + TypeScript + Vite 6；本地构建由 FastAPI 同源提供 |
| 数据访问 | SQLAlchemy + pyodbc + ODBC Driver 18，已验证 Windows 本地身份连接 |
| 助手存储 | 当前仅使用 Git 忽略的本地 JSON 开发快照；独立 SQL Server 助手库仍为目标 |
| 后台任务 | 当前独立 CLI 生成快照；持久化任务状态、调度与恢复待实现 |
| AI | 模型通过受控工具获取数据并解释结果；供应商与部署方式待评测 |
| 飞书 | 官方 Python SDK `lark-oapi` 已加入后端锁定依赖；仅环境准备，客户／群聊推送和调度未实现 |
| 测试 | pytest + Ruff；TypeScript 构建及 Playwright 浏览器测试 |

当前实施以方案第 15 节的 pandas 架构为准。第 14 节讨论的分布式计算、专用分析引擎和事件管道仅用于未来容量评估。新增基础设施前应有实际业务或性能依据，并记录决策原因。

**模块边界与规划目录**

取数层按明确范围提供数据；pandas 分析函数接收数据和参数，返回结构化指标、图表数据及证据标识；FastAPI 校验权限、读取结果及提交任务；模型理解问题并调用这些业务能力。

| 规划位置 | 用途 |
|---|---|
| `backend/app/api/` | HTTP 接口与请求校验 |
| `backend/app/core/` | 配置、认证、日志 |
| `backend/app/db/`、`connectors/` | 源库只读连接、助手库读写连接、业务表映射 |
| `backend/app/metrics/`、`analysis/` | 指标定义、pandas 函数与异常规则 |
| `backend/app/ai/` | 模型适配、工具参数、结果校验 |
| `backend/app/insights/`、`tasks/` | 事项聚合、运营待办与反馈 |
| `backend/app/schemas/` | API 输入输出契约 |
| `backend/workers/` | 调度、按需同步、分析与晨报 |
| `backend/migrations/` | 仅管理助手库结构 |
| `backend/tests/` | 单元、集成与问数评测样例 |
| `frontend/src/` | TypeScript 客户端、页面和组件 |

当前已有 `backend/app/analysis/`、`connectors/`、`core/`、`schemas/`、`backend/workers/`、`backend/config/` 和 `backend/tests/{unit,api,integration}/`，以及 `frontend/src/` 和 `frontend/tests/`。首批 HTTP 路由暂集中在 `backend/app/main.py`；其余目录仍属规划。PowerShell 摸底与启动工具位于 `scripts/`。实际目录或接口变化后，同步更新 README 和本文。

**数据与计算约定**

- 源库连接使用授权的只读对象；助手库读写连接单独配置。账号、密码、模型密钥和真实企业数据通过运行环境管理，不写入仓库或日志。
- 在 SQL 中先限制必要列、日期和授权范围，参数化传入值，避免直接拼接用户输入。查询范围和字段许可由服务端决定。
- 数据接入保留主键、源更新时间、统计截至时间和同步水位，处理取消、退款、软删除与旧订单变更。
- pandas 函数保持输入、参数和输出明确；当前时间、阈值和比较周期显式传入，便于复算。
- 金额沿用 ERP 精度，使用 Decimal 或经确认的整数最小单位；检查读取过程中的浮点转换。日期与业务时区、币种和单位均有明确口径。
- 主订单与供应商子订单分别计数；支付、退款和明细等一对多关系先按正确粒度处理，再关联，检查重复键和空键。
- 分块读取适用于可分解的任务。跨块去重、均值、全局排名和采购间隔按完整业务范围计算，不能简单拼接或累加分块结果。
- 记录任务耗时、读取行数和峰值内存。通过缩小读取范围、优化类型、复用结果和有界分块满足当前容量要求。
- 计算结果保留指标版本、数据水位和证据；复用结果时同时匹配权限、参数和时效。同步不完整时明确显示范围与状态。
- 耗时 pandas 分析在独立后台进程执行，API 返回任务标识和状态。调度入口避免多进程重复启动；任务重试和结果写入保持幂等。

**AI 与权限约定**

模型可调用的工具映射到已定义分析函数，工具参数和输出经过校验。金额、数量、比例和排序来自确定性计算，模型解释中引用相应指标与证据。

采购企业、供应商和平台运营角色的数据范围由后端执行。列表、详情、图表、会话、缓存、证据和导出采用一致的权限规则，不能通过更改企业 ID 或复用另一身份的会话访问数据。

数据缺失、查询失败或模型超时时明确说明状态。模型不可用时，仍可展示已计算的事实与模板摘要；缺少因果证据时表述为变化贡献或待核实线索。

**当前文档验证方法**

从仓库根目录执行：

```powershell
git status --short
git diff --check
git diff --cached --check
```

检查新增和修改文档的相对链接能否解析到仓库内文件，代码围栏是否闭合，技术决策是否一致，规划功能是否标注清楚。审查暂存内容，避免遗漏新增文档或带入无关文件。`git diff --check` 不覆盖未跟踪文件；新增文件加入暂存区后再执行 `git diff --cached --check`。

文档检查与业务测试分别报告，不应把文档空白检查报告为应用测试通过。

**当前后端测试方法与后续测试约定**

从根目录 `.venv` 安装 `backend/requirements.lock` 后，在 `backend/` 运行：

```powershell
..\.venv\Scripts\python.exe -m pytest tests/unit -q
..\.venv\Scripts\python.exe -m pytest tests/api -q
..\.venv\Scripts\ruff.exe check .
$env:ERP_RUN_INTEGRATION = '1'
..\.venv\Scripts\python.exe -m pytest tests/integration -q
Remove-Item Env:ERP_RUN_INTEGRATION
```

三个测试目录均已建立。集成测试默认跳过，只在显式设置环境变量后读取本地恢复的测试库，不写源库。默认单元和 API 测试使用固定合成样例，不依赖数据库或外部模型。Windows 沙箱临时目录权限异常时可为 pytest 指定项目 `.local/` 下的新 `--basetemp` 和 `-o cache_dir=...`，不要清理未知临时目录。

前端在 `frontend/` 运行 `npm run build` 和 `npm run test:e2e`。本机可设置 `PLAYWRIGHT_CHANNEL=chrome` 使用已安装浏览器；默认测试只使用合成记录，不连接 ERP。不要把真实企业名称、原单、令牌或截图写入受版本控制的测试用例。截图和跟踪文件保存在 `.local/`。

以下矩阵同时列出当前和后续所需覆盖；已有订单指标、关联对账、暂定规则、开发令牌、快照、源库读取和看板测试。支付退款、增量、助手库写入、调度与 AI 尚未实现，不能宣称已通过对应测试。

| 测试层次 | 验证方式 | 重点场景 |
|---|---|---|
| pandas 单元测试 | 小规模固定数据，手工计算期望值；验证输入不被意外修改 | 成交额、净额、去重计数、趋势、复购、空数据、零基数、日期边界 |
| 关联与增量测试 | 构造主子订单、分次支付和状态变更，重复执行同步与重算 | 多表关联不放大金额，退款校正历史结果，重复事件不重复计数 |
| FastAPI 接口测试 | 测试客户端配合模拟取数和模型返回 | 参数校验、权限、数据时间、证据、分页、任务状态和失败响应 |
| SQL Server 集成测试 | 在专用测试库执行真实驱动读取与助手库写入 | Decimal、空值、时间类型、事务、超时、只读权限与幂等写入 |
| 后台任务测试 | 注入异常并模拟重复领取、重试和进程恢复 | 单次有效执行、进度恢复、取消、失败记录、重复晨报 |
| AI 问数评测 | 固定问题和工具结果，分别核对工具选择、参数和答案 | 指标与周期正确、事实有引用、缺失数据不补写、权限不足正确处理 |
| 前端验证 | 框架确定后增加类型检查和相应测试 | 加载、空结果、失败、过期数据、任务进度、筛选及图表一致性 |

SQL Server 特有行为需要在 SQL Server 测试环境验证。真实模型评测使用脱敏样例单独运行，记录模型、提示和工具版本，并把模型差异与确定性计算错误分别报告。

**首批对账样例约定**

以下为合成样例，所有支付和退款事件在同一统计期内，币种与单位一致；此处成交订单数指发生成功支付的主订单数，净额指本期支付金额减本期成功退款金额。

| 主订单 | 采购企业 | 商品明细与供应商 | 成功支付 | 成功退款 | 状态 |
|---|---|---|---|---|---|
| O1 | A | S1：60；S2：40，共两个供应商子订单 | 两次支付 40 + 60 | 0 | 已支付 |
| O2 | A | S1：200，一个供应商子订单 | 200 | 50 | 部分退款 |
| O3 | B | S1：90，一个供应商子订单 | 0 | 0 | 未支付且取消 |

期望支付成交额为 300，退款为 50，净额为 250，成交主订单数为 2，成交供应商子订单数为 3，成交采购企业数为 1。针对 O1 关联两个明细与两条支付记录后，支付总额仍应为 100。

继续补充跨月退款、同一企业跨日采购、缺失日期、空键、重复事件和排序边界；各测试根据业务口径独立定义期望值，避免用待测算法再次生成期望结果。

**性能与人工验收**

在脱敏且具有代表性的数据上记录读取范围、行数、查询时间、计算时间、进程峰值内存及结果一致性；复用结果与重新计算分别测量。容量目标依据真实业务量和可接受延迟确定，不预设固定行数的承载保证。

业务人员抽查趋势、排行和企业采购历史，沿证据回查订单；确认 Top 5 有效性并记录漏报或误报。任务完成与业务效果改善分别验证。

**交付与协作约定**

- 技术选型和指标口径变化时，同步维护 README、本文和相关方案；待定事项不能写成既有实现。
- 验证报告记录实际执行的命令、结果与未覆盖范围。文档改动执行文档检查，业务改动运行相关且有意义的测试。
- 提交前检查差异、相对链接和暂存内容；沿用当前任务已获得的提交／推送授权，保持正常 Git 历史，不擅自强制推送。
- 当前用户消息明确调用 `$superpowers-entry` 时，才按用户约定使用 Superpowers 入口；单独提及某个 `superpowers:*` 技能不绕过此限制。
