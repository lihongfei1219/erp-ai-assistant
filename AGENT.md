# 项目开发与协作约定

当前进度以[项目进度](docs/project-status.md)为准；模块架构见[架构规划](docs/erp-ai-assistant-plan.md)，入口见[README](README.md)。本文件只维护长期协作约束，不再追加每轮实施经过。

## 范围与技术决策

- 当前任务范围是只读数据分析，不执行ERP业务操作。独立云端语义层接销售、已完成销售退货、已确认销售出库、商品批次库存执行器。销售原六类分析保留；新域范围及数据口径见架构与数据字典。库存仅有备份时点，全平台授权才可查询，不能假定采购企业拥有全局库存权限。
- Python由uv项目管理：根目录`pyproject.toml`声明依赖、`uv.lock`锁定版本、`.python-version`选择3.11，使用`uv sync --locked`和`uv run`；不再维护独立requirements锁文件。技术栈为FastAPI、Pandas/Decimal、Pydantic AI、LangGraph、SQLAlchemy/pyodbc；前端React/TypeScript/Vite仍由npm及package-lock.json管理，网页Plotly本机脚本，飞书原生卡片2.0。
- ERP来源为本机SQL Server恢复库`ERP_Local`，业务源只读。用户确认备份覆盖全平台商家。分析使用`.local/reports`快照，飞书任务／结果／发件箱使用独立`ERP_AI_Assistant.erp_ai.feishu_sales_jobs`。
- 飞书应用机器人已有授权群和用户，`scripts/start_feishu_bot.py --sales`启用问数；日报Webhook是另一通道，尚未启用真实定时推送。
- 日常统一入口为根目录`uv run python start.py`，默认网页8001及飞书问数共用同一快照；`--check`仅做离线检查。子进程沿用当前Python环境，Ctrl+C一起停止，任一子进程退出则回收同次启动的另一服务。两个单服务入口保留，禁止按进程名批量停止其他实例。
- 部署配置统一由`core/environment.py`读取根目录`.env`，进程环境优先，CLI可覆盖监听地址／端口／快照；相对路径基于项目根目录，`ERP_ENV_FILE`可由进程指定其他文件。禁止默认读取`.local`中的旧飞书JSON、Webhook凭据或签名密钥；旧JSON仅显式`--config`兼容。迁移工具先校验再原子写入，确认后删除已迁移的旧配置，运行状态不得删除。`.local`中的快照、会话和去重记录仍属持久化数据；部署参数见`.env.example`。
- 飞书 `user_access_mode` 默认 `allowlist`；用户已授权当前内部群启用 `all_group_members`，只免除群内逐人白名单，应用／租户／群和有效用户身份校验、发送前复核及按用户隔离追问仍保留。不得把空白名单解释为全员开放。
- 用户允许按已记录的常识默认规则继续开发，不因一般口径待确认反复停工。基础设施升级需业务或性能证据，不预先增加分布式引擎、多Agent或队列平台。

## 数据与模型边界

- 执行能力统一注册在`backend/app/capabilities/registry.py`；`view.py`按快照覆盖、完整性标记及授权生成当前能力视图。模型元信息、API目录、计划校验及引导共用该来源；`semantic-catalog.json`仅维护可表达的业务词汇与口径，不重复维护执行白名单。能力版本和覆盖变化使旧会话／建议失效，不能静默迁移旧选择。
- 商品／客户对象筛选已接入，库存仅支持商品。`semantic/entities.py`从当前授权域事实本地绑定编码；唯一完整名称可直接用，同名或部分匹配必须确认，未找到不能静默忽略或计算为零。同字段包含取并集，排除优先，跨字段取交集。商品筛选后金额只计匹配明细、单据去重；图表、下载与`/analysis/evidence`使用相同筛选。候选目录、已绑定编码与内部选择动作不传云端（用户原话中的编码除外）。

- SQL先限制日期、必要字段和权限范围，参数化查询；助手连接每次验证数据库名，严禁向ERP_Local写任务或迁移。
- 金额使用Decimal，计算与快照保留四位；飞书展示两位不改变事实。订单、明细、支付、退款分清粒度，主明细必须对账，避免关联放大。
- 有效状态暂定订单完成、已出库；CNY、Asia/Shanghai暂定。不是支付成交额，未扣退款。规则位于`backend/config/business-rules.json`，不确定事项只记[业务假设](docs/business-assumptions.md)。改规则后生成新快照，不能覆盖旧结果。
- 数据水位和完整日期明确展示；缺失或部分日不能补成完整零销售。当前完整日期09-01至09-15，来源截至2026-09-16下午。
- “卖得好／畅销”默认金额前10，显式指标优先。用户确认“药品”泛称按当前商品范围分析，必须标注未按药品类别筛选、可能包含其他商品；明确分类、对象、数量等条件不能忽略。
- 模型只接收当前问题、日期、业务定义、结构化会话状态，以及服务端裁剪的能力和完整日期元信息；不接收ERP结果、名称清单、身份或原始对话历史，不生成SQL、Python或卡片代码。能力元信息中的全平台授权仅为布尔边界，不含企业身份；内部允许动作不交模型推断渠道编号。新语义路径由模型理解意图，不维护客户问法词典或用中文关键词二次裁决。`compiler.py`校验结构化能力、合并上下文并转换计划；授权与数据范围由程序控制。模型须完整保留限制并标记歧义，理解准确率及供应商超时通过独立评测持续观察，不保证所有表达都理解正确。旧注入式计划器仍由`analytics_bounds.py`检查。
- 自由对话通过共享`analysis/dialogue.py`和`/analysis/converse`渐进补全。正常缺信息、能力缺口、日期缺口是HTTP 200引导；每轮一个关键问题，输入保持开放。目标、约束、待解决事项按稳定ID处理，未提到的条件保留；换指标、删条件、改显式日期或缩减组合须有明确用户语义或已绑定选择。建议动作从签名上下文验证，不把客户选项拼成固定问句重新调用模型。
- 凭据只放私有`.env`或环境变量，不打印配置值、令牌及原始模型响应，不把真实业务记录写入测试或截图仓库。`.env.example`只放空值或虚构示例，单元测试不得读取开发者真实`.env`。

## 运行与恢复

- `.local/feishu-app`含进程锁、发现记录和连接测试去重状态；助手库另存正式问数任务。不得为了清缓存删除状态记录、批量重置`unknown`或盲目重发。
- 飞书模型调用前保存中断标记并移除原文，结果先落库再发送。发送前重新校验授权；明确拒绝与未知送达区分处理。
- 飞书追问按应用／租户／群／用户及同快照隔离，仅继承成功送达计划或语义澄清；30分钟按当前请求入队时刻判断，清除按请求顺序生效。网页为本机免登录入口，不生成或校验本地访问令牌；网页会话仍绑定快照和语义配置，私有签名密钥使用`.env`的`ERP_CONTEXT_SIGNING_KEY`（32字节十六进制），迁移时保持原值，不得发送给浏览器。
- `/converse`上下文或动作失效返回409，网页保留可见草稿，用户明确恢复后才作为新问题重建，禁止静默重用失效状态。飞书完整引导先持久化再发送；卡片明确说明编号以当前用户最新提问卡片为准，旧卡片不阻挡新选择。仅最近语义任务为成功送达且仍在30分钟窗口内的提问卡片时可选；更新的未完成任务、送达不确定、清除上下文或过期均阻止旧编号执行。通过编号成功应用的轮次保留审计记录；当前无卡片点击回调和引用卡片编号定位。追问卡片首屏只突出一个问题和少量选项，完整条件默认折叠，候选绑定与实际动作保持一致，不截掉移除条件、改口径或对象编码等关键含义。
- 本地多服务可能同时存在。停进程前核对PID、启动命令和工作区；机器人升级前检查在途任务。隐藏启动使用`Start-Process -WindowStyle Hidden`。
- `.venv`、`frontend/node_modules`、`frontend/dist`及数据库安装目录可能在用，不视为普通缓存。缓存清理先预览，校验绝对路径在工作区内，拒绝重解析点；不要整体清空`.local`或使用`git clean -fdx`。

## 开发、验证与交付

- 模块职责见架构文档，新增统计先有可信输入与结果契约；数值和权限由程序控制，图表只转换绘图坐标。
- 默认pytest使用合成数据，不连接数据库或模型。`ERP_RUN_INTEGRATION=1`是源库只读测试；`ERP_RUN_ASSISTANT_TESTS=1`仅操作随机合成身份的助手任务；`ERP_RUN_MODEL_TESTS=1`显式模型评测。开关不能混为一般离线检查。
- 后端从项目根目录运行`uv run pytest`和`uv run ruff check backend`；前端从`frontend/`运行构建及Playwright。Windows测试可指定新的`.local/`临时目录；Chrome可通过`PLAYWRIGHT_CHANNEL=chrome`使用。
- 修改计算或接口需相应回归；只改文档／缓存时检查链接、围栏、失效引用、受保护文件哈希、依赖导入和运行健康，不把这些检查称为全量业务测试。
- 文档进度只更新`docs/project-status.md`，验证汇总到`docs/validation.md`；使用方法写对应说明。完成的阶段计划及时合并，不堆积一次性文档。
- 检查`git diff --check`、文档相对链接、未跟踪文件；不清理其他人的源码改动，不擅自提交或推送。真实消息送达、模型联调、本地预览和业务验收分别报告。
