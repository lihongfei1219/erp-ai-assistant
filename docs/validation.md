# 验证与维护记录

更新：2026-09-23。合并原阶段1／2、网页分析、飞书连接、日报、排行、自然语言及卡片展示的验证记录。保留结果、关键修复和未覆盖范围；删除重复命令、过时限制和中间截图路径。

15:36按用户要求停服：在途任务0；仅停止已核对工作区和启动命令的网页、飞书机器人进程，核验对应PID均退出且8001连接关闭。以下启动和健康记录均为停服前验证。

## 统一能力注册（2026-09-23）

注册22种现有分析，移除语义配置、执行域常量、计划模型及提示元信息中的重复执行白名单；保留用于理解尚未实现请求的业务词汇。API、模型和飞书帮助共享按快照、完整日期、质量标记与权限裁剪的能力视图。新增36项契约测试，逐项执行注册能力及全部指标，覆盖销售概览订单数的语义／执行兼容、组合限制、库存授权、缺少事实、数据不完整、元信息不含业务记录以及能力覆盖变化使旧绑定失效。

后端最终分批验证覆盖755项唯一测试：723 passed、32 skipped。完整批次先通过131项，因旧测试仍匹配“未接入”文案而停止；调整为验证“事实未加载”和`facts_missing`后，重跑语义API及全部unit／integration目录，593 passed、32 skipped；按JUnit测试ID合并并覆盖复测结果得到上述总数。初轮迁移检查发现旧上下文默认语义版本未跟随注册表，已改为当前目录版本工厂；跨Python进程恢复在最终回归中通过。迁移期间因源码／配置同时调整而中止的测试批次不作为通过证据。记录为`.local/registry-final-20260923.xml`和`.local/registry-remainder-20260923.xml`，仅有既有Starlette测试客户端弃用提示。

隔离状态目录的离线回放27场景／36轮全部通过，报告`.local/registry-evaluation-20260923.json`；不代表云端理解准确率。真实云端合成抽测中文日期排行与跨域组合修改／追加库存，2场景4轮全部通过，耗时8.360～11.015秒，无供应商失败或依赖跳过，报告`.local/registry-cloud-20260923.json`。仅使用合成问题／快照，不上传真实ERP记录，也不向飞书发送测试消息；该小批次不代表持续准确率或延迟承诺。

实际本地四域快照检查22项能力可用、对象筛选仍为false；能力版本`erp.capabilities.v1.55d4f09a13452d81`。Ruff、文档相对链接与差异空白检查通过。飞书重启前独立助手库仅有10条sent、无在途任务；只停止核对命令和工作区后的旧机器人，隐藏启动新版本并确认语义启用、问数就绪和长连接建立，无告警或标准错误输出。网页未运行，本轮未启动；没有改写ERP、重置历史任务或推送代码。升级后旧会话／建议需重新开始或显式恢复，真实客户端新版走查仍待用户验证。

## 飞书语言引导改进（2026-09-22）

新增独立的结构化引导展示模块，标题区分缺信息、能力缺口及数据缺口；保留模型已理解内容、约束和默认条件，提示再次@、自由补充及上下文时限。日期提示绑定待确认目标，含结束日期的展示不越过实际覆盖范围；库存不宣传跨日或实时查询。日期选择器仍隐藏，展示和数字动作共用同一筛选顺序；编号不明确时只提示文字选择。帮助及数据范围按实际已加载和授权域生成，销售快照或非全平台授权不会宣传可查全局库存。结果追问文案移除可能不适用于库存的固定销售建议。

最终离线回归67 passed，覆盖新引导、四域帮助、授权范围、纯文本展示、连续追问、数字建议、持久化恢复、去重以及分析展示；相关文件Ruff与差异空白检查通过。使用合成数据，无真实模型调用或主动测试消息。重启前只读检查助手库：9条sent，无在途任务；核对并停止本项目原机器人后隐藏启动新版本，确认语义启用、问数模式就绪、长连接建立，无连接告警及标准错误输出。未修改ERP、授权或历史任务状态，网页进程未停止；新引导的真实客户端外观和云端自由表达理解效果仍待用户走查。

## 飞书无响应与配置诊断（2026-09-22）

检查Python进程未发现飞书Worker，存在网页进程；离线`--check`因缺少应用ID失败。本地配置文件、当前进程飞书凭据环境变量均缺失，根`.env`也没有这两个凭据键；检查仅输出存在性和服务类别，不输出凭据或消息。用户确认无备份后创建空`.local/feishu-app.json`，`git check-ignore`确认不纳入版本控制；未填入或推测任何租户、群和用户授权。

配置加载器增加“文件不存在且环境凭据不完整”的明确提示，保留环境凭据启动发现／探测模式及显式授权要求。环境检查新增飞书配置未就绪提醒，文档明确网页与机器人分别启动。配置、发现、权限和去重等单元测试15 passed；相关Python文件Ruff、环境检查和差异空白检查通过。没有启动机器人、连接飞书、调用模型或发送消息；真实连接和收发仍待用户填写配置后验证。

后续用户填写凭据后：离线检查确认应用字段已填，但尚无群／用户授权；真实`--probe`成功读取机器人身份。核对无现存飞书Python进程后，隐藏启动`--discover`，确认启动进程存活、长连接已建立、无连接告警及标准错误输出。发现记录尚未产生，等待用户群内@事件；没有发送消息，也未启动正式问数或检查助手任务库。后台启动信息保存在私有`.local/feishu-app/discovery-process.json`。

用户确认已发送发现测试后，收到唯一一条与当前应用匹配的群／用户记录；仅将该记录写入本地授权，保留应用凭据。正式模式切换前只读检查独立助手数据库及任务分布：8条sent，无在途任务；四域快照可执行、全平台范围和云端模型启用。核对发现进程树时首次因Windows控制台宿主不匹配而安全停止操作，随后单独核验并停止发现模式Python及uv，未影响网页。隐藏启动`--sales`后确认进程存活、凭据验证通过、四域语义启用、任务模式就绪及长连接建立，无连接告警和标准错误输出。启动信息保存在私有`.local/feishu-app/sales-process.json`。发现测试不会回放成问数任务，尚待用户新问题验收真实模型及卡片回复；未主动发送测试消息，未改变业务计算代码。

## 恢复缺失的启动快照（2026-09-22）

用户运行uv启动入口时遇到`Report file not found`。确认三个默认快照均不存在，本机`MSSQL$ERPLOCAL`正常运行；以Windows当前用户身份连接并核对数据库为`ERP_Local`。沿用已授权全平台范围、2026-09-01至09-17左闭右开提取区间，以及09-16 14:33:41+08:00备份水位，执行已有有界只读查询，生成`.local/reports/platform-four-domain-20260922.json`。没有修改ERP记录、覆盖旧快照或使用合成数据代替业务数据。

销售SQL控制总量对账、退货／出库单据及数量对账、库存数量对账和完整模型重新加载通过。TestClient验证`/healthz`、网页首页、分析目录均为200，四域能力可执行；同一请求执行销售金额、退货金额、出库数量和备份库存四项概览，返回200且有四项结果。验证输出仅保留通过状态，不打印业务明细。本轮未调用模型或发送消息，也未留下长期监听的业务进程；用户可按原uv命令自行启动。下方缺失快照记录为修复前状态。

## uv项目管理迁移（2026-09-22）

将Python项目配置迁至根目录，新增原生`uv.lock`和`.python-version`（3.11），移除旧backend配置及requirements锁文件。现有应用按非分发项目管理，保留根目录`.venv`和Python启动脚本；安装、启动、测试文档及PowerShell辅助入口改用`uv sync`／`uv run`。pytest路径和Ruff源码目录显式指向backend，避免移动配置改变测试发现和导入分组。实现依据[uv项目结构](https://docs.astral.sh/uv/concepts/projects/layout/)与[锁定／同步说明](https://docs.astral.sh/uv/concepts/projects/sync/)。

原75项已安装依赖版本逐项核对保持不变。uv.lock包含77项：原有包、虚拟根项目及额外平台专用依赖；不会将该平台专用包安装到当前Windows环境。临时迁移约束已移除。`uv lock --check --offline`、`uv sync --locked --offline`、环境导入／时区／ODBC／前端产物检查、网页和飞书Python入口help均通过；根目录uv执行启动和图恢复测试18 passed，收集711项测试，Ruff通过。PowerShell脚本语法、文档链接和差异空白检查通过。本轮没有重跑完整业务回归或前端构建，也未启动业务服务、访问ERP或真实模型。

数据快照缺失仍由本地检查独立提示；包管理迁移不创建业务数据。此前安装依赖及LangGraph验证记录保留如下，历史requirements命令不再作为当前安装入口。

## 启动依赖补齐（2026-09-22）

将运行时直接导入的httpx、httpx2、OpenAI兼容客户端、LangSmith和Starlette列入生产依赖；httpx从dev组迁入dependencies。重新解析锁文件，所有75项包版本与迁移后锁文件一致，没有升级现有库。新增`Setup-Environment.ps1`安装／构建入口及`check_environment.py`本地检查，README和使用说明同步更新。

实际运行一键脚本：Python 3.11.15、Node 20.18.0、npm 10.8.2；Python同步及兼容检查75项通过，npm ci成功，TypeScript／Vite构建通过；运行时模块、LangGraph SQLite检查点、Asia/Shanghai时区、SQL Server ODBC 18与前端产物检查通过。启动和图恢复冒烟测试18 passed；脚本Ruff、文档链接及`git diff --check`通过。本轮未重新运行全量业务测试、访问ERP、调用真实模型或发送消息。

当前工作区缺少默认`.local/reports`快照。检查脚本单独提示数据未就绪，安装脚本不生成模拟业务快照。要启动真实业务分析，仍需恢复已对账快照、指定已有文件，或另行从恢复库生成新快照。

## LangGraph主对话链路迁移验证（2026-09-22）

已安装并锁定`langgraph==1.2.12`、`langgraph-checkpoint-sqlite==3.1.1`及传递依赖，解析时约束原锁文件，未升级原有依赖。网页`/analysis/converse`与飞书自由问数接入真实StateGraph，模型适配、四域计算及发件箱边界保留。下方“架构选型核对”是此前仅设计阶段的历史记录，不代表当前仍未实施。

验证覆盖：SQLite检查点跨新事件循环与独立Python进程恢复、签名日期选择不再调用模型、重复请求复用结果、同ID异内容冲突、同thread并发与旧轮次拒绝、身份／渠道／快照／过期隔离、模型失败后显式新请求、旧签名草稿迁入、引擎回退需显式恢复、过期检查点清理、检查点不含原始问题和业务行。旧分支测试改为各自导入独立会话，以适应“一个待答轮次只能接受一次后续修改”的约束。

最终后端全量回归679 passed／32 skipped，包含17项迁移专项；唯一警告为既有Starlette测试客户端的AnyIO弃用提示。Chrome浏览器23 passed，覆盖明确模型失败后使用新请求编号、网络结果不明时沿用原编号、日期选择、草稿恢复和输入保留。Ruff后端检查、前端构建和文档链接／差异检查通过；`uv pip check`确认75个已安装包兼容，原55项依赖版本全部保留，新增20项。离线标注评测27场景／36轮全部通过，报告`.local/langgraph-evaluation.json`，不代表真实云端模型准确率。

浏览器首次测试的3处完整请求断言因新增`request_id`不匹配，已更新并补充编号行为断言。Windows测试服务自动回收阻塞，后改用独立受控合成服务复测；一次从错误工作目录运行导致截图路径不存在，纠正为frontend目录后全量23项通过。结束后仅关闭已核对命令／目录的测试服务进程，确认8765端口不再监听。

本轮未调用真实云端模型、连接ERP数据库、发送飞书消息或启动／重启业务服务。浏览器使用独立的合成数据测试服务。尚未验收统一能力注册、可组合计算DAG、对象筛选、生产多实例协调和云端理解准确率提升。

## LangGraph架构选型核对（2026-09-22）

用户明确选择LangGraph后，核对官方Overview、Graph API、Interrupts和Persistence文档，将设计调整为StateGraph流程编排；模型生成的业务计算DAG与运行图分开。方案明确专用等待节点、检查点和恢复、同thread串行与版本校验、最多2次总模型预算，以及现有供应商接口／四域执行器／飞书发件箱的复用边界。内存检查点仅用于单元测试，本机试用拟使用各渠道独立SQLite文件；进程重启前尚未解释且未保存的用户原文不保证自动恢复。

本轮仅更新架构、进度和验证三份文档；没有安装LangGraph、变更锁定依赖、建立检查点数据库或切换业务路径。文档相对链接、围栏与差异空白检查通过，业务代码未变，未重跑业务测试；官方机制核对不是项目集成验收。正式实施须先通过计划中所列中断／恢复、并发、版本失效、预算及飞书幂等专项测试。

## 通用规划架构设计核对（2026-09-22）

用户在推进对象筛选过程中明确要求先重新设计架构。本轮核对语义契约、云端输入、编译器、业务能力常量及对话分支，确认现有运行流程由模型理解自由表达，并非按种子题库匹配；不足在于固定操作映射缺少可执行依赖关系，以及能力来源分散。方案新增目标草稿／逻辑计划、统一能力视图、对象本地绑定、结构化诊断、有限计划修复、增量迁移与冻结验收设计。

已撤回本轮尚未接通的ObjectFilter契约和对象匹配模块；业务代码与已推送提交一致。仅修改既有架构、进度与验证文档，同时修正本地令牌旧描述及评测数量。检查文档相对链接、Markdown围栏及`git diff --check`；未运行新的后端／浏览器测试、云端调用、数据库访问或消息发送，不能将此设计记录视为新能力已经实现。

## 销售升序与追问方向（2026-09-22）

定位旧销售执行器固定降序、计划校验与语义编译拒绝升序、能力引导只提供降序替代，以及销售理解说明写死方向。现支持商品／客户按金额或订单数升降序，先对全部分组排序再截断，同值按编码升序；结果与图表顺序一致，并注明不包含期间无有效销售记录的对象。非排行仍不接受升序，允许用户明确取消排序，其他业务的分析能力引导保持优先。

新增回归先复现5个失败，修复后覆盖最低项、订单证据、图表、客户排行、同值顺序和连续往返切换保留日期／指标／条数。离线评测扩展为27场景／36轮，全通过（`.local/evaluations/ranking-full-replay-20260922.json`）；回放不代表模型准确率。独立审查补验四位Decimal差异、空区间、取消排序与其他业务引导，通过。

云端仅使用合成场景：受限网络初次2次连接失败、2轮依赖跳过；网络放行后4轮中3通过，截图原句出现多余指标澄清，原记录保留在`ranking-cloud-network-20260922.json`。补充通用规则，销售表现高低在未指定衡量方式时同样采用业务默认，并明确本轮相反方向覆盖旧方向。最终`ranking-cloud-defaults-20260922.json` **2场景／4轮全部通过**，含截图“九月十五号什么药品卖的少”和按订单数降序→升序→降序连续追问；耗时2.906～9.704秒。这是定向验证，不代表任意表达均稳定。

最终全量后端 **662通过、32跳过**，既有Starlette弃用警告1项；Ruff及差异空白检查通过。语义配置升级v4，旧签名草稿走既有过期／恢复流程，不能直接复用旧建议。未改前端、未重跑浏览器、未发送飞书消息或读写ERP源库；业务服务未启动或重启，使用者重新启动后端后生效。

## 提问后保留输入框（2026-09-22）

移除自然语言请求成功后的自动清空，保留用户输入用于查看、修改和再次提交。既有浏览器场景补充结果返回、能力引导和重试成功后的输入值断言；清除上下文仍可主动清空。

TypeScript检查及Vite构建通过；Chrome全套 **22通过**，含连续追问、重试、日期建议、过期恢复和四域展示。仅前端行为改动，未重复后端／云端测试。合成测试服务在验证后关闭；本轮未启动或重启业务服务，结束检查时8001已有服务监听，未停止该服务。

## 列举目标、日期和复合筛选联调（2026-09-22）

本轮补跑此前未覆盖的16个云端场景，初次为10场景通过、6场景失败：20轮中13通过、6失败、1依赖跳过，含2次约25秒超时。没有把有效JSON或正常HTTP响应当成语义通过。报告为`.local/evaluations/dialogue-cloud-remaining-20260922.json`；其中包含后续确认的标注等价问题，不能直接作为最终模型准确率。

定位并修改通用提示定义：退货对象列举不能退化为整体概览；已给日期而数据未覆盖不能当作缺日期；明确包含和排除条件须分别保留，不能按猜测的分类关系省略所谓冗余条件。未新增问法词典、执行器能力或业务写入。补充退货列举→对象／日期补全、列举与汇总对照，以及两种排除表达，用例扩展为 **25场景／32轮**。

评测按业务默认指标和排行默认对象核对等价语义，仍拒绝用默认金额替代明确数量、用商品替代明确地区。退货原句可先问对象或日期，人工备选仍要求保留list目标并最终返回商品列表。合成库存场景改为明确前十，以单独验证历史库存后追问当前时点，不强迫模糊极值句接受隐藏的前十标签。

- 全量后端 **652通过、32跳过**，既有Starlette弃用警告1项；Ruff、差异空白及文档相对链接检查通过。新增回归包括默认等价边界、合理澄清备选及不能遗漏排除条件。
- 最新独立离线回放 **25场景／32轮全部通过**，记录为`dialogue-replay-expanded-20260922.json`，不等于云端模型准确率。
- 中间定向复测暴露过出库额外澄清、筛选条件丢失和超时，均保留原报告，没有覆盖或改写失败结果。最终提示修改后的批次`dialogue-constraints-final-20260922.json`为 **5场景中4通过，6轮中5通过、1超时**；退货原句及补充轮、今天出库、原始类别筛选和口语排除通过，反序排除改写25.125秒超时。本批已返回结果的轮次没有意图偏差，仍不声称所有表达均稳定或超时已经解决。
- 最终云端通过轮次耗时12.578～16.922秒；整批含超时P50为13.313秒、P95为25.125秒。筛选仍是能力缺口，当前执行器不会带这些未支持条件计算业务结果。
- 独立只读审查验证合理备选没有放宽明确指标、地区和唯一极值约束；6条额外错误变异均被拒绝。未改前端、连接器或执行器，未重跑浏览器构建和源库集成；未启动业务服务、生成访问令牌或发送飞书消息。

## 自然语言标注评测（2026-09-22）

新增独立的`backend/evaluation/`及`scripts/evaluate_dialogue.py`，21个场景、27轮；13个截图通用原句和8个合成补充场景分别标记来源。评测集不接入运行提示词或问法匹配，默认离线，显式`--cloud`才调用模型。测试报告只记录状态、枚举类型、失败字段和耗时，不含问题原文、业务结果或凭据。

- 全量后端 **642通过、32跳过**，既有Starlette弃用警告1项；Ruff和差异空白检查通过。新增33项测试覆盖语义／计划偏差检测、依赖失败跳过、供应商故障脱敏、拒绝真实快照、离线默认入口及禁止覆盖报告。
- 离线参考语义回放 **21场景／27轮全部通过**；这验证对话和执行链路，不代表模型理解准确率。
- 云端最终抽测 **5场景／7轮全部通过**：唯一极值对象引导、退货存在性、中文日期排行、地区能力缺口、三域组合→仅改出库→追加独立日期库存。耗时3.797～15.015秒，P50为8.125秒，P95为15.015秒；本批未超时，不代表持续无超时。
- 初次抽测3场景／5轮中有2轮被评测错误判为指标不一致。独立审查确认库存`stock/quantity`等价表示，以及地区问法未明确指标、不应强制订单数。补充先失败后通过的回归后修正计分；同时修复“哪个最好”遗漏`limit=1`导致错误接受前50名。没有为样例新增问法规则或修改业务提示词。
- 首轮、最终回放与云端报告分别保存在`.local/evaluations/`下，最终云端文件为`dialogue-cloud-reviewed-20260922.json`，含模型与提示词／配置／用例集哈希。选取少量云端场景，未将其通过率描述为整套模型准确率。
- 本轮不修改前端或执行器，未重复浏览器构建和业务源库测试；未启动业务服务或发送飞书消息，结束时8001关闭且访问令牌文件不存在。

## 本地免令牌入口（2026-09-22）

移除网页登录、本机API的Bearer校验和启动脚本的访问令牌生成；删除`.local/api-token.txt`，会话私有签名密钥保留且删除前后哈希一致。网页清除旧浏览器访问令牌，接口请求不再携带Authorization。

- 后端全量 **609通过、32跳过**，既有Starlette弃用警告1项；Ruff通过。无令牌API、启动不生成令牌、会话签名与快照范围均覆盖。
- TypeScript检查及Vite构建通过；Chrome **22通过**，覆盖直接进入、旧令牌清除、订单明细、四域展示和连续引导。
- 测试使用合成数据，不调用ERP、云端模型或真实飞书发送。业务服务保持关闭。

## 四域组合引导修复（2026-09-22）

补查发现退货／出库比较请求误进入销售订单数专用建议，且“改看概览”保留分组会继续报能力缺口。现建议保留业务域、指标、统计日期及原排行／明细对象；选择整体概览会明确说明取消分组及条数限制，退出比较也在选项中说明。另修复组合后续目标的模型日期候选误按第一项目标日期覆盖校验的问题，库存候选只接受自身快照范围。

- 新增21项合成回归，覆盖三个新增域、金额／单据数、客户／商品、有无条数及排序限制、模型日期候选与未解决问题，缺陷先复现失败再修复。每个候选分别验证后续执行与其他目标不变。
- 独立复核指出的两项残留已修复：非排行建议明确取消排序；取消比较仅解除同一目标被取消字段的普通问题，不清除依赖、其他目标或未指定目标的问题。四域API定向46通过，独立复核46通过。
- 最终全量后端 **608通过、32跳过**；Ruff、差异空白检查通过，保留既有Starlette弃用警告。
- `scripts/probe_operations_model.py --probe --flow recovery`：**8通过、0失败、0跳过**。退货／出库比较请求均返回对应操作能力缺口；两次云端理解分别14.39和10.80秒，六个选项均在本地成功执行且不再调用模型。只使用独立合成报告，未读真实快照或主动发送飞书消息。
- 评测脚本`all`现共21项；本轮只运行新增8项，原13项记录保留在下节，不宣称本轮单次21项全套通过。未改前端和来源读取，未重复浏览器全套及源库集成。
- 15:34核验在途任务为0后重启网页与机器人；首页／健康检查200、飞书长连接建立且stderr为空。运行中四域本地API组合检查通过，错误库存日期仍返回422。

## 四域连续对话联调（2026-09-22）

新增四域签名会话回归，验证不同目标保留各自日期、选择库存快照不重调模型、仅修改退货不改变另外三项、目标ID稳定、旧建议不能用于新轮次。合成云端脚本扩展为13项场景，核对完整计划与结果域，前置失败会标记跳过并返回非零。

联调发现当前库存的时间证据问题：模型曾将“现在”改为备份日期，本地校验阻止执行但退回补日期；进一步复测模型输出`today`，原日期解析未支持该符号。现保留当前时点语义，支持已有日期表达中的“现在／当前／此刻”和模型`today`符号，并验证原文证据；不将快照当作当前时间。原文有显式历史日期时，“现在帮我看某历史日”仍按该历史日执行。独立审查发现追问省略日期或复制旧日期会继承备份，已补充先失败后通过的回归并阻止静默执行；复核11通过。

- 全量后端：**587通过、32跳过**，既有Starlette弃用警告1项；Ruff、差异空白检查通过。
- 云端合成验证分批完成：常规单域、组合修改、追加目标、补日期、客户追问、能力缺口等10项通过；最后库存定向3项全部通过，耗时17.25秒、0.00秒（选项无模型调用）、18.05秒。此前失败和跳过未计为通过；诊断期间另有一次25.22秒供应商超时，仍不能保证持续无超时。
- 本轮没有前端或数据连接器改动，未重复浏览器全套或源库集成；此前四域浏览器22通过见下节。
- 15:20在途任务为0后重启网页及飞书机器人。首页和健康检查200，飞书长连接建立且stderr为空；四域本地真实API执行通过，库存错误日期仍返回422。未主动发送飞书测试消息。

复测入口：`scripts/probe_operations_model.py --probe --flow all`；库存定向使用`--flow inventory`。云端仅使用独立构造的合成报告，不读取真实快照。

## 四域执行器接入（2026-09-22）

- 全量后端：**576通过、32跳过**，保留既有Starlette弃用警告。覆盖分域计算、四位Decimal、批次合并、分单位数量与排行、来源键隔离、库存日期／权限、缺数据与空数据、组合和会话建议。
- 固定只读源库集成：**1通过**，独立启用`ERP_RUN_INTEGRATION=1`。核验退货、已确认销售出库、库存控制总数及主明细／单位对账。新快照已生成，原快照不覆盖，源ERP库无业务写入。
- 云端合成联调：`scripts/probe_operations_model.py --probe`，三个域全部得到`result`，耗时6.67、6.84、6.25秒。脚本只构造合成事实，不加载真实快照。基于真实快照元信息的外部模型探测曾被自动审批拒绝，因此改用合成云端联调和本地真实数据执行分别验证；没有绕过拒绝发送真实派生信息。
- 网页：TypeScript／Vite构建通过；Chrome **22通过、退出码0**，包括新域单位、标题、精确数量、库存时点和不会误跳销售证据。Windows测试服务退出阶段等待较久，确认仅测试fixture子进程归属后停止该子进程，runner正常结束；未停止业务服务。保留既有Lucide构建提示。
- 独立审查修复：新域概览／趋势／有无记录中显式对象分组不再丢失，用户可改同对象排行；客户明细保留客户维度，超限明确展示总分组数和截取说明；受限企业失联或客户不一致的退货单、同时缺原销售单与确认日期的出库单不得静默变成零记录，新增执行实际SELECT的合成SQL回归；飞书明细保留所选金额、数量和单位。最后两项边界先复现失败再修复，定向21通过，并已纳入全量。
- 09-22 15:01在途任务0后重启网页和机器人，加载`platform-four-domain-20260922.json`。首页／健康检查200，飞书长连接建立且stderr为空；本地真实API执行销售＋退货＋销售出库＋库存四域组合通过，错库存日期返回422。未主动发送飞书测试消息，实际客户端视觉验收仍由用户完成。

退货／销售出库的完整日期09-01至09-15；库存仅为09-16 14:33:41备份时点。退款、退货率、仓库映射、历史库存／周转及指定对象筛选不在本次已接能力内。后端命令：`../.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider`；前端：`npm run build`、`PLAYWRIGHT_CHANNEL=chrome`后运行`npm run test:e2e`。上述云端耗时仅是本次样本，不保证没有超时。

## 地区维度与连续超时排查（09-22此前阶段）

用户原句“九月十号哪些地区成交频繁”连续重试仍超时。安全日志确认连续5次约25.1秒失败；按旧协议复现时为首次请求25.36秒超时，尚未收到响应头，不能解释为第二轮结构修复超时。离线验证同时发现旧`target`只能表达商品／客户，`region`会触发结构校验失败；省略维度则可能错误落到商品排行。

语义维度已改为非空、最多80字符的开放名称，配置只定义常见标准ID及实际可执行维度。模型可完整表达地区、其他维度及分组与筛选的区别；执行器仍只接受已支持的商品／客户维度。未支持维度返回`capability_gap`，保留日期、指标及组合，不擅自执行商品排行。客户明确选择替代维度后才转换；原操作不能按维度输出时，建议明确改为排行。

修复后同句非流式云端调用连续3次通过，耗时9.86、8.64、11.14秒；均只有一次请求，正常完成，`target=region`且无额外疑点。仅用于定位的流式探测在1.48秒收到响应头、1.50秒收到思考事件、10.62秒收到正文事件、11.42秒完成。只记录阶段、计数、token数量及校验状态，未记录思考／正文、密钥或业务数据。生产仍沿用原调用方式和25／30秒等待边界，没有关闭思考或添加网络重试；这些样本不证明所有超时均被消除。

最终全量后端 **538通过、31跳过**，既有弃用警告1项；Ruff、差异空白与前端构建通过。新增维度专项覆盖开放协议单次返回、地区／任意名称、不支持操作与组合、保留草稿、显式改维度及确实按维度执行的替代建议。独立只读审查未发现执行权限扩大。浏览器本轮只增加维度标签，未重跑上一轮完整21项浏览器测试。

14:05再次核验在途任务为0后更新网页和机器人。真实网页接口对原句返回HTTP200、`capability_gap`、保留地区维度和两个替代建议，耗时22.62秒；未输出业务结果。首页／健康检查200，飞书长连接建立且stderr为空。云端耗时存在波动，应与协议正确性分别记录，不宣称永久无超时。复现入口：`scripts/probe_guided_dialogue.py --probe --flow dimensions`。

## 渐进引导与会话编辑（09-22追加）

网页及飞书使用共享`converse`决策，正常缺信息、能力缺口与数据缺口返回引导，保留自由输入。新增稳定目标／条件／疑点ID、明确增删改、追加目标、待定位修改、受校验的签名选项、日期选择、重复澄清提示与过期后显式恢复。未增加客户问法词典、第二次文案模型调用或ERP写入。

| 验证 | 最终结果 | 覆盖 |
|---|---|---|
| 全量后端 | **523通过、31跳过**，既有Starlette弃用警告1项 | 原功能及语义、引导、权限、选择绑定和恢复 |
| 引导API | 16通过，计入全量 | HTTP200四状态、自由补全、选项零模型调用、日期缺口、显式删筛选、组合依赖、条数与可恢复摘要 |
| 语义状态专项 | 81通过，计入全量 | 稳定ID、原子编辑、追加目标、日期继承／待定位日期核验、未提条件保留 |
| 浏览器 | 全套Chrome **21通过**；最终引导7项定向复测通过 | 自由补充、日期动作、超时重试、过期恢复、迟到回包、移动端及中文排序 |
| 前端构建 | TypeScript／Vite通过 | 保留原有Lucide构建提示 |
| 独立助手库 | **20通过**，单独启用 | 合成身份，唯一编号绑定、跨身份隔离、未知送达和清除屏障 |
| 静态检查 | Ruff与`git diff --check`通过 | 后端、新评测脚本及修改文件 |

31个默认跳过项为8个旧模型测试、20个助手库测试、3个源库只读测试。助手库测试使用合成身份并清理自身记录；本轮未访问ERP源库或主动发送飞书测试消息。浏览器测试使用隔离合成快照；飞书真实客户端仍需用户试用验收。

云端验收使用`scripts/probe_guided_dialogue.py --probe`，全部输入及报告均为合成数据。首轮8项中7通过、取消筛选1次超时；`--flow filter`两轮定向复测均通过，取消后保留原日期并执行。追加目标联调发现复述共同日期被错误清空，修复后`--flow append`三轮全部通过：概览2.70秒、追加排行10.00秒、仅修改新增目标5.77秒。另有6项旧语义重点回归中5通过、口语排行1次多余澄清，该口语项单独复测通过。没有将中间失败称为全量云端通过，也没有放宽25秒HTTP／30秒整次等待限制。

独立审查发现并修复了比较改趋势残留比较期、恢复草稿丢失关联限制、追加目标覆盖旧目标、追问条数被忽略等边界，均补充行为回归。结果代表当前合成验收，不保证任意自然语言均一次理解成功；后续仍需积累脱敏真实问题并分别观察理解质量与服务可用性。

11:53重新启动项目，重启前在途任务为0，仅停止经工作区及启动命令核验的旧服务。首页和`/healthz`均200；真实`/converse`输入宽泛销售需求返回200待补日期（12.97秒），选择签名日期后200完成销售概览（0.03秒，无第二次模型调用）。仅记录状态、步骤类型与耗时，未输出业务结果行或令牌。飞书四域语义已启用、长连接建立、stderr为空；网页地址仍为 http://127.0.0.1:8001 。

## 云端模型超时修复（09-22追加）

定位到新语义适配器遗漏原接口的思考模式参数；当前百炼模型默认使用高强度思考，而HTTP等待为25秒。同题对照：原默认25.22秒超时；关闭思考1.88秒返回。但关闭思考的19项回归中17通过、2项组合指标理解失败，所以没有上线该中间配置。

最终对官方百炼已确认支持的型号显式启用低强度思考，保持现有超时和零网络重试。组合问句对照为：关闭思考2.55秒但指标错误，低强度思考10.89秒且指标正确。最终9项针对性云端回归全部通过，覆盖原句、变体、两类组合、筛选、日期澄清及补全、订单数／数量追问。没有将测试问句加入运行提示词。

全量后端 **450通过、26跳过**，Ruff及差异空白检查通过；既有弃用警告1项。实际HTTP请求契约验证供应商域名和型号分支、参数传递，错误分类测试验证超时链、鉴权、限流、上游异常及无效结果；共20项专项通过。日志只含固定原因类别与耗时，不含异常正文、问题或凭据。独立审查通过。

网页及机器人已更新重启，重启前在途任务为0。此记录说明本轮验证结果，不保证所有云端请求都不会超时；后续失败可以从安全分类日志定位。

重启后的网页真实接口验证：截图原句HTTP200、9.67秒、返回一组商品金额排行；商品口语排行与客户订单数排行组合HTTP200、9.47秒、返回两组正确指标结果。首页与健康检查200，机器人长连接建立、stderr为空；未主动向飞书发送测试消息。

## 模型主导意图理解（09-22追加）

用户试用时，口语排行被模型不稳定地判成数量指标。最初复现的两条合成口语问法均返回unsupported；根据用户进一步要求，最终修复移除了运行配置中的问法别名、提示词中的问句示例、中文正则意图裁决，以及从原话关键词推导排行条数的规则。问句仅保留在评测中。模型根据业务定义输出结构化意图，程序校验能力、日期、上下文和授权后执行。

后端全量回归 **434通过、26跳过**，Ruff通过；保留既有Starlette/AnyIO弃用警告。新的失败回归确认过口语误拦、组合排行条数污染与范围继承丢失，修复后通过。新增结构化范围拒绝、药品范围说明和追问继承检查；独立审查发现的两项条件丢失已修复并定向复核通过。旧的“模型漏字段后靠中文关键词兜底”测试改为结构化契约测试；不能再将它们描述为原文遗漏的确定性保障。

语义配置升为v2，旧网页上下文需清除。网页和飞书服务已重启，机器人重启前在途任务为0，长连接已建立。未主动向飞书发送测试消息，未修改ERP源库。

最终合成云端回归19项：14项通过，5项因模型服务超时／暂不可用未通过，不能称为全量云端通过。此前定向回归已验证订单数及数量追问保留；截图原句在新抽象提示词下返回ready。超时发生在口语变体、组合、筛选、日期补全和订单数追问，接口返回503且不继续执行业务查询。网页首页及健康检查200，语义理解准确率与云端可用性分别记录。

服务重启后，原句“九月五号那天什么商品卖的多”第一次网页接口复测遇到503，云端评测结束后的单独复测返回200，实际执行9月5日商品金额前10名，返回一组结果；未将业务数值写入日志或文档。

## 独立语义接口初版（09-22）

| 验证 | 本轮结果 | 范围 |
|---|---|---|
| 后端全量离线回归 | 422通过、26跳过；既有Starlette/AnyIO弃用警告1项 | 原功能兼容、供应商替换、四域语义、中文日期、组合、会话与接口 |
| Ruff | 后端及新合成评测脚本通过 | 静态检查 |
| 前端构建 | TypeScript及Vite通过 | 保留既有Lucide构建提示 |
| 浏览器端到端 | Chrome 15通过 | 原页面功能、新增澄清令牌继承及显式清除 |
| 独立助手库 | 15通过 | 合成任务、已送达澄清持久化、未知送达排除和清除屏障 |
| 云端语义合成问法 | 11项均有通过记录；最终全量10通过、1次服务暂时不可用，该澄清补全项定向复测通过 | 四域、单日／今天、组合、数量／筛选边界、补充日期；不读ERP |
| 独立代码审查 | 发现项已修复；最终五个定向回归独立复核通过 | 筛选不丢失、日期补全、金额／订单数及各目标指标不被替换 |

26个默认跳过项为8个旧模型测试、15个助手库测试、3个源库只读测试。助手库15项已单独启用执行，只使用合成身份任务并清理自身记录。本轮没有访问ERP源库或发送飞书消息，生产服务保持停止。浏览器测试使用独立合成快照服务。

新语义层云端评测命令为`.venv/Scripts/python.exe scripts/probe_semantic_model.py --probe`；定向补全复测加`--case pending`。云端偶发不可用说明可用性仍需持续观察，服务失败不会继续执行业务查询。最终文档本地链接、差异空白及Ruff检查通过；8000、8001、5173与测试8765端口均关闭，无飞书机器人进程。

新增回归覆盖缺日期前的具名条件核对、多目标缩成单目标时保留筛选、补比较期保留原日期、相对日期固定、上下文过期／篡改／快照变化、失败送达不继承，以及跨重启继续追问。原文条件核对仍是受控词汇与规则保护，合成测试通过不能证明任意自然语言都能正确理解；后续继续积累脱敏真实问法评测。

## 上一版功能证据（09-20）

以下来自对应功能实施及本次提交前的实际执行记录。提交前重新执行离线后端回归、Ruff、前端构建和Chrome端到端测试，未重复调用模型、数据库或飞书。

| 验证 | 最近记录 | 范围 |
|---|---|---|
| 后端全量离线回归 | 提交前378通过、25跳过；既有Starlette/AnyIO弃用警告1项 | 六类分析、接口、任务、新旧卡片兼容及补充边界回归 |
| 展示专项最后回归 | 15通过 | 另补超大金额坐标抑制、截短趋势不画完整图 |
| Ruff | 后端及合成预览脚本通过 | 静态检查 |
| 前端构建 | TypeScript及Vite通过 | 保留既有Lucide构建提示 |
| 浏览器端到端 | Chrome 14通过 | 看板、日报、分析、自然语言原句及手机布局 |
| 独立助手库 | 14通过 | 合成身份任务、去重、租约、恢复、追问和清除屏障 |
| 真实模型合成问法 | 网页13项、飞书流程8项通过 | 工具、日期、指标、排名、范围限制和追问 |
| 飞书展示预览 | 13组样例、桌面／手机26份截图通过 | VChart 1.12.3渲染、翻页、折叠、无页面横向溢出 |

25个默认跳过项为8个模型测试、14个助手库测试、3个源库只读测试。展示预览中的表格和容器是近似实现，不能替代飞书客户端验收；历史截图和可重建预览已纳入本次缓存清理。

飞书应用凭据、长连接及授权群收发已验证，用户确认过真实商品排行回复。统一卡片上线前在途任务为0，重启后凭据有效、六类功能启用、长连接建立、stderr为空。统一卡片本轮没有主动向群发送测试消息，实际新版视觉仍待确认。日报Webhook真实发送和定时推送尚未启用。

## 重要历史里程碑

| 日期／阶段 | 已执行结果 |
|---|---|
| 09-16 数据接入 | 697表元数据摸底、主明细／SQL控制总数对账；55项测试通过，含3项源库只读集成 |
| 09-16 经营看板 | 76项后端测试、8项浏览器测试；新旧快照分别保留，规则和指纹可追溯 |
| 09-20 日报 | 97项后端通过、3跳过；9项浏览器通过；网络使用替身，未实际发日报 |
| 09-20 飞书排行 | 173项通过、6跳过；首次3项助手库集成通过；用户确认真实排行送达 |
| 09-20 三类模型问数 | 282项通过、9跳过；6项助手库和6条真实模型合成问法通过 |
| 09-20 网页六类分析 | 首次324项通过、9跳过；自然语言改进后348项通过，14项浏览器及13项模型评测通过 |
| 09-20 飞书六类分析 | 363项通过、25跳过；14项助手库和8项模型流程通过 |
| 09-20 统一展示 | 最近证据见上表；未改变计算或权限逻辑 |

历史真实快照走查验证过主明细金额相等、概览与趋势合计相等、商品排行与独立计算逐项一致；业务明细不复制到验证文档。小范围试点的响应时间不代表生产容量。

## 已修复的关键问题

- 模型可能忽略原问题条件：先独立识别日期、指标、工具、条数和不支持条件，再核对计划；不执行被静默缩小的查询。
- 金额和图形：Decimal保留四位，飞书显示两位；网页图形用编码区分同名对象，飞书图形使用独立序号；零基数和无订单均值不伪造0。
- 网络与持久化：调用模型前保存中断标记并移除原文；未知调用不重试。飞书明确拒绝记`rejected`，不确定送达记`unknown`，不自动重复发送。
- 飞书授权重载移到领取回复前，避免配置短暂不可读时搁置`ready`任务。
- 追问按当前消息入队时间和同身份／同快照判断30分钟有效期；清除按请求顺序生效，避免排队或发送顺序导致错误继承。
- 原连接测试曾收到飞书业务码`99991672`，检查`im:message:send_as_bot`权限和应用版本生效后，新消息回复成功；旧不确定状态未批量重置。

## 复现命令

默认后端验证不联网、不读写数据库，从`backend/`执行：

```powershell
../.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider
../.venv/Scripts/ruff.exe check . --no-cache
```

数据库和模型测试须分别显式开启：`ERP_RUN_INTEGRATION=1`只读恢复后的源库；`ERP_RUN_ASSISTANT_TESTS=1`只写独立助手库的合成任务并清理自身记录；`ERP_RUN_MODEL_TESTS=1`发送合成问题但不发送业务结果。具体命令见[飞书问数](feishu-analytics.md)。不要在默认回归中开启全部外部调用。

从`frontend/`执行`npm run build`，设置`PLAYWRIGHT_CHANNEL=chrome`后执行`npm run test:e2e`；测试服务使用合成快照和8765端口。飞书本地预览复现见[展示说明](feishu-analytics.md)。

## 工作区整理（09-20）

本次范围为文档合并及可重建缓存清理，不修改业务代码、配置、数据或运行进程。文档从26份收敛到10份；原18份阶段设计、执行清单、分散验收及旧版问数说明的有效内容合并到现有入口。

实际清理21,721个可重建文件，逻辑大小395,281,623字节（约377MiB）；`.local`顶层条目收敛到29个。65个被运行中Python占用的uv缓存文件保留，约43.7MiB。历史测试目录的访问身份不同，先预览再分批清理，没有修改目录权限或停止服务。

清理后135个源码、配置、快照和构建文件哈希与基线一致；关键Python依赖导入通过，55个已安装包兼容检查通过；8000／8001的`/healthz`均200，原有网页、Vite及飞书进程仍存活。全部12个根目录及docs文档的本地链接、围栏和空白检查通过。没有重复调用模型、访问源库或发送群消息。

清理脚本经独立审查，修复PowerShell祖先对象类型、异常进度记录和目录重解析点遍历问题。合成目录验证了缓存内日志删除、被独占文件保留、解锁后删除空目录，以及凭据／快照／依赖环境／Git跟踪文件拒绝；最终普通缓存预览0候选。运行中锁文件不读取内容、不删除。

从根目录预览清理清单；只有显式`-Apply`才删除：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/Clean-Workspace.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/Clean-Workspace.ps1 -IncludeDownloadCaches -Apply
```

脚本使用明确的生成物名称／目录规则，校验路径位于本工作区、拒绝重解析点及受Git跟踪的文件；保留`.local/reports`、`.local/feishu-app`、凭据、运行日志、PID、`.venv`、`node_modules`、`frontend/dist`和`database_backups`。无法读取或正在占用的项目保留并记录；下载缓存内部的npm安装日志属于可清理生成物。`ExecutionPolicy`只作用于本次命令，不改系统策略。不使用`git clean`或清空整个`.local`。清理报告只记录路径、数量和逻辑字节；下载缓存可能与已安装依赖共享硬链接，逻辑大小不等于实际释放磁盘空间。

文档整理后检查本地链接、代码围栏、失效引用及Git差异；缓存清理后验证关键文件哈希不变、依赖可导入、API健康和已有进程存活。业务功能验证仍以上述功能版本记录为准。
