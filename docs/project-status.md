# 项目进度与下一步

更新：2026-09-22。这里是进度的唯一维护入口；功能操作和验证证据分别链接到专门文档。

## 当前结论

飞书端语言引导已完善并更新到运行进程：引导按已理解需求、默认条件、单个待确认问题、建议及回复方式分段；明确继续@、保留条件和30分钟上下文。日期提示使用当前待确认目标的完整范围，库存说明备份时点；重复澄清和编号歧义提供文字补充出口。“帮助／数据范围”根据快照和授权展示四域状态，移除旧的三域未接入文案。模型仍负责理解自由表达，本轮未新增问法词典或改变执行口径。67项相关回归通过；无在途任务时已重启飞书并确认长连接、语义及问数模式就绪，新引导实际客户端体验待用户测试。

本次飞书无响应排查已恢复连接：代码入口及LangGraph接入保留，原因为本地配置缺失且机器人未启动。用户重新填写应用凭据并在目标内部群发送发现测试，已取得唯一群／用户记录并完成授权。助手库检查通过，切换前仅有8条已发送任务、无在途任务；四域快照及模型配置就绪。现已停止发现模式并隐藏启动正式`--sales`，确认语义理解启用、问数模式就绪、飞书长连接建立，无连接警告。等待用户发送新的自然语言问题验收实际回复；网页与机器人仍为独立进程。配置缺失诊断及环境检查提醒已补齐。

Python包管理已切换到标准uv项目：根目录`pyproject.toml`／`uv.lock`／`.python-version`，日常使用`uv sync --locked`和`uv run python scripts/start_backend.py --port 8001`。旧backend依赖配置和requirements锁文件已移除，现有根目录`.venv`与Python入口继续复用；前端仍用npm。

本次启动环境核对：已补齐运行时代码直接使用的HTTP客户端、OpenAI兼容客户端、LangSmith追踪控制和Starlette依赖声明，保持所有已锁定版本不变；新增`scripts/Setup-Environment.ps1`安装／构建入口和`check_environment.py`本地检查。启动时缺失的四域快照已通过本机`ERP_Local`只读查询重新生成到`.local/reports/platform-four-domain-20260922.json`，销售及三域主明细／数量对账、重新加载、网页和四域组合执行检查均通过；源业务记录未修改。现在可直接使用上述uv启动命令。

已完成可在本机运行的四域只读分析助手，以及供应商可替换的独立云端语义接口。网页和飞书现已共用渐进引导：自由表达、展示已理解条件、每轮一个关键问题、可选建议及条件修改；正常引导不再作为错误。销售、退货、出库、库存均有语义配置；销售、退货、销售出库和库存均已接入执行器，以当前快照是否包含对应事实为准。当前处于内部试用阶段，数据仍来自历史备份，尚未达到持续更新、正式权限和进程守护齐备的生产部署状态。

用户当前范围是“通过自然语言获取数据分析结果”，不执行 ERP 业务操作。今日Top 5、运营待办、业务写入不列入近期实施。

LangGraph主对话链路迁移已落地：网页`/analysis/converse`和飞书自由问数共用StateGraph，拆分读取本轮、模型理解、编译、校验、执行、响应及等待节点。SQLite保存结构化检查点；专用`interrupt`等待节点以`Command(resume)`恢复。会话绑定渠道、身份、快照、语义／图版本与30分钟有效期；签名句柄仅带会话引用，重复请求可复用结果，同轮并发和旧建议被拒绝。现有供应商适配、四域计算和飞书发件箱继续复用；本机访问无需登录令牌。

这是[通用架构重设计](erp-ai-assistant-plan.md#通用任务规划架构重设计分阶段实施)的编排迁移阶段。统一能力注册、可组合业务计算DAG和对象筛选仍按后续顺序推进；换用LangGraph本身不代表未知指标、任意关联计算或自然语言准确率已经解决。

按用户要求移除本地访问令牌和登录页面，已删除`.local/api-token.txt`；Python和PowerShell启动入口均不再创建或校验访问令牌。网页直接进入工作台，连续追问仍使用服务端签名上下文。网页及飞书业务服务由用户自行启动；最近输入框改动验证结束时8001已有服务监听，本轮未启动、重启或停止该业务服务。

网页提问提交后保留输入框原文，用户可直接编辑并再次查询；结果、引导及重试成功不再清空文字。前端构建已更新，刷新／关闭页面后的草稿持久化不在本次改动内。

## 已完成与验收边界

| 模块 | 当前进度 | 仍需关注 |
|---|---|---|
| 数据接入 | SQL Server恢复库只读取数、主明细对账、不可覆盖快照 | 尚无自动增量同步 |
| 业务口径 | 原始订单／暂定有效销售分离，Decimal四位精度，规则版本与指纹 | 支付退款事实尚未接入 |
| 网页 | 经营概览、订单明细、业务口径、飞书日报、AI数据分析 | 本机直接访问，未接正式SSO |
| 自然语言 | 独立供应商协议；模型基于四域业务定义理解意图，无问法词典；稳定目标／条件ID、单问引导、显式增删改、签名建议与四域执行 | 分类、指定对象筛选、销售数量、利润、资金退款／退货率、历史库存趋势／周转未接入；模型理解准确率与超时需持续评测 |
| 飞书问数 | 应用机器人长连接、群／用户白名单、持久任务和发件箱、30分钟追问 | 需电脑和进程持续运行 |
| 飞书展示 | 卡片2.0、图表、分页表格、折叠说明、空数据和容量降级 | 本地预览通过；新版实际客户端视觉验收待确认 |
| 日报 | 预览、Webhook发送实现、独立08:00调度入口 | 未启用实际定时推送，缺每日完整新数据 |
| 工程验证 | 后端、浏览器、助手库、真实模型合成评测均有记录 | 未建立CI和生产压测基线 |

新增`/analysis/converse`统一返回结果、待补充、能力缺口与数据缺口；网页支持日期选择、自由补充、已知条件编辑与过期后显式恢复。飞书先持久化引导再发送，仅唯一待答轮次允许数字选择，多轮不猜编号。当前百炼型号保留低强度思考与原超时边界，模型故障保留草稿供用户重试。各轮验证及云端偶发失败详见[验证记录](validation.md)。

当前完整分析日期为 **2026-09-01至2026-09-15**。来源截至2026-09-16 14:33:41，当日不完整。“最近一周”始终按提问日解释，不自动替换成备份日期。

已修复分析对象只能表达商品／客户的语义限制：地区及任意业务维度可完整保留，当前不可执行时说明维度能力缺口，并由用户明确选择是否改按商品／客户分析。截图原句云端连续3次通过；09-22 14:05已再次更新网页和机器人，运行接口实测正常能力引导（22.62秒），长连接建立。云端耗时仍有波动，不能承诺不再超时。

“卖得好”默认金额前10；用户已确认“药品”泛称按当前商品范围分析并标注未做类别筛选。显式要求“只看药品／排除器械”不能忽略。订单金额不是支付成交额，未扣退款。

销售商品／客户排行已支持金额及订单数升降序；低值排行先排序再截断，并明确不包含期间无有效销售记录的对象。模型通用定义支持相反方向覆盖旧条件，指标缺省对高低表现同样处理；已移除执行层和引导层的销售升序限制，说明文字与结果方向一致。语义配置升级v4，旧签名会话需按页面提示显式恢复或重新提问。新增截图低值原句及三轮排序切换，评测累计27场景／36轮；最终定向云端4轮通过，首次多余指标澄清的失败记录保留。

新增四域快照`.local/reports/platform-four-domain-20260922.json`：退货和销售出库完整日期为09-01至09-15；库存仅有2026-09-16 14:33:41（Asia/Shanghai）时点，不是日末或实时库存。数量分单位统计／排行，显式分组不能静默忽略。原销售快照保留。

四域连续对话已补充组合后改单项、追加独立日期库存、自由补日期和签名建议回归。当前库存按提问时点引导数据缺口，须明确选择才使用备份；看过历史库存后再问当前库存，不得静默继承旧日期。合成云端场景分批通过，仍记录到供应商偶发超时，详见验证记录。

组合请求的引导建议按待补目标校验日期覆盖；退货／出库不支持期间比较时，可以明确改看概览、原对象排行或明细。建议保留原指标及其他目标，取消比较、分组或条数限制时在选项中说明；期间比较本身尚未接入。

自然语言种子评测已扩展为27场景／36轮，含14个截图通用原句和13个合成场景；覆盖日期、四域、筛选保留、连续补全、改单项、追加目标和升降序切换。云端扩大覆盖后定位退货列举被误判概览、缺覆盖日期被当成缺日期、明确排除条件被视为冗余而遗漏的问题；已补充通用输出形态、日期边界及逐条保留筛选定义，没有加入固定问法。退货原句→澄清对象→明确商品与日期→列举的云端流程已通过。供应商仍观察到约25秒超时，分批结果和后续复测见[验证记录](validation.md)，不能合并重复重试为总体准确率。

统一入口`scripts/evaluate_dialogue.py`记录离线／云端结果、失败字段、实际澄清字段、依赖跳过、耗时和版本哈希。已支持人工审核的合理澄清备选，默认指标与排行对象按业务语义比较，显式数量、范围和唯一极值限制仍严格校验；详见[评测说明](analysis-assistant.md#自然语言标注评测)。

## 下一步顺序

| 优先级 | 工作 | 完成标准 |
|---|---|---|
| 已完成 | LangGraph主链路与恢复机制 | 依赖锁定；网页／飞书接入；SQLite独立进程恢复、重复请求、并发冲突、失败重试、旧会话迁入及回退验证通过。详见验证记录 |
| P0-1 | 统一能力注册与当前能力视图 | 先注册现有能力；模型提示、API目录、计划校验和引导共用一个来源。快照覆盖、权限和数据粒度进入服务端能力视图；现有查询结果不变 |
| P0-2 | 图节点接入目标草稿、逻辑计划与结构化诊断 | 保留无法绑定的新目标，表达计算依赖和约束覆盖；现有简单查询已由图执行，下一步扩展通用计划契约。区分缺信息、缺数据、缺能力与系统故障，网页／飞书沿用既有响应，支持按新会话灰度与回退 |
| P0-3 | 对象筛选与选择闭环 | 本地名称／编码匹配，歧义对象可选择；商品明细金额、去重单据数、图表和证据同范围；跨轮增删改条件不丢失。作为新架构首项能力扩展 |
| P0并行 | 扩充评测并建立冻结验收集 | 当前27场景／36轮，扩充目标约100场景；开发与验收样例分开，覆盖新表达、计划组合和约束，独立统计语义偏差与供应商故障。门槛见架构设计 |
| P1 | 销售数量、分类及首个关联任务 | 先核验单位和类别映射，再注册能力；合法商品键关联销售和库存，先对齐粒度与期间，明确零销售全集与历史库存边界 |
| P1 | 飞书真实客户端走查 | 同一组概览、排行、趋势、比较和组合问题，在桌面／手机确认可读、数值和范围一致 |
| P1 | 持续更新数据 | 确认授权源、增量水位和删除／历史修正语义；定时生成对账通过的新快照，显示新鲜度 |
| P1 | 运行维护 | 单实例进程守护、重启恢复、日志轮转、助手库备份／保留期、告警；CI执行离线检查 |
| P1 | 正式访问控制 | 接平台账号、SSO及数据范围；查询、证据、导出和报告链接采用一致权限 |
| P2 | 扩展四域指标 | 基础执行器已接入；后续接资金退款、明确退货率分母、仓库及历史库存，再计算周转 |

下一步完成P0-1至P0-2的统一能力和通用计划契约，再接对象筛选；持续数据来源可并行确认，恢复库重复取数不能带来新的业务记录。正式访问控制用于后续多人部署，本机免令牌入口维持用户当前约定。LangGraph已以库方式嵌入现有后端并通过本机持久检查点验证，飞书消息仍走既有发件箱。没有性能实测依据时继续使用Pandas，不预先增加分布式计算、多Agent或额外队列系统。

## 日常入口

- [README](../README.md)：安装、启动及目录导航。
- [架构与实施规划](erp-ai-assistant-plan.md)：模块职责、数据边界及后续实施条件。
- [网页分析](analysis-assistant.md)、[飞书问数](feishu-analytics.md)、[应用接入](feishu-app-setup.md)、[日报](feishu-daily.md)：使用与运维。
- [数据字典](data-dictionary.md)、[指标定义](metrics-v1.md)、[业务假设](business-assumptions.md)：事实和口径。
- [验证记录](validation.md)：最近一次验证及历史里程碑；测试通过不等于生产或真实群验收。

09-22 15:36按用户要求关闭网页及飞书机器人；停止前在途任务为0，已核验对应进程退出、8001端口不再监听。用户可从项目根目录运行`uv run python scripts/start_backend.py --port 8001`，然后打开 http://127.0.0.1:8001 ；需要飞书问数时，在另一个终端运行`uv run python scripts/start_feishu_bot.py --sales`。两个服务均默认加载四域快照，停止时分别在终端按Ctrl+C。启动脚本默认端口8000，日常命令显式指定8001；已有前端构建由后端同源提供。

## 文档与文件维护

文档收敛为10份：本页、架构、4份使用说明、3份数据口径文档和1份验证记录。已完成的阶段计划、重复设计及分散验证记录合并后删除，不再每轮新增一份计划和一份验收文档。只有新增独立子系统且确需单独设计时才增加文档。

`.local/`不是可以整体删除的缓存目录：其中包含会话签名密钥、飞书凭据、进程锁、去重状态和真实快照。仅清理明确可重建的测试临时目录、截图、HTML／JSON预览、文档抓取副本及依赖下载缓存。保留源代码、锁定依赖、已安装环境、前端构建、数据库备份与安装介质。

本次已清理21,721个可重建文件，逻辑大小约377MiB；保留65个被运行中服务占用的uv缓存文件（约43.7MiB）。135个受保护文件哈希未变，已有服务健康检查通过。清理命令及复查方法见[验证记录](validation.md)，本机汇总及最近批次明细保存在`.local/maintenance/cleanup-last.json`；不包含密钥或业务记录。
